#include <iostream>

 using namespace std;
 
int main() {
    
    int a,b;
    cin >> a >> b;
    int x = a + b;
        cout << "X = " << x << endl;
        cout << "X = " << a - b << endl;
        cout << "X = " << a * b << endl;
        cout << "X = " << a / b << endl;

    int c,d;
    cin >> c >> d;
    int y = c - d;
        cout << "Y = " << y << endl;

    int e,f;
    cin >> e >> f;
    int z = e * f;
        cout << "Z = " << z << endl;

    int g,h;
    cin >> g >> h;
    int w = g / h;
        cout << "W = " << w << endl;

    return 0;
}